# 차세대 SEMI-LIS 🚀

### 설치

```bash
# 의존성 설치
pnpm install
```

### 개발 서버 실행

```bash
# Vite 개발 서버 시작 (권장)
pnpm run dev

# 또는 간단한 실행
pnpm start

# live-server 사용 (대안)
pnpm run serve
```

개발 서버가 실행되면 브라우저에서 `http://localhost:3000`으로 접속할 수 있습니다.

### 빌드

```bash
# 프로덕션 빌드
pnpm run build

# 빌드 결과 미리보기
pnpm run preview
```
